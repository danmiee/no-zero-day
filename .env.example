# Frontend environment variables (Vite)
# Copy to .env and fill in values.

# Backend API base URL.
# - Leave empty in local dev (Vite proxy forwards /api → localhost:3001).
# - Set to your deployed backend URL in production.
VITE_API_BASE_URL=
